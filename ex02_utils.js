function upper(text) {
    return text.toUpperCase();
}

module.exports = { upper };

// tudo que escreve tudo dentro do modulo é apenas visivel no modulo
// oque é xportad pra fora é aquilo que atributo que percente ao modulo ex module.export